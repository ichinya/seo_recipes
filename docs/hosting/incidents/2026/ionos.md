---
title: Инциденты IONOS Cloud в 2026 году
description: Сбои Managed Kubernetes, provisioning и ограничения емкости IONOS Cloud в 2026 году
icon: fa-solid fa-triangle-exclamation
category: Хостинг
tag: [Хостинг, Инциденты, IONOS, IONOS Cloud, "2026", Kubernetes, Provisioning, Емкость]
provider: ionos
year: 2026
---

# Инциденты IONOS Cloud в 2026 году

- [Карточка IONOS](../../providers/ionos.md)
- [Все инциденты 2026 года](./)
- [Официальная status-панель](https://status.ionos.cloud/)

Последняя проверка: **19 августа 2026 года**.

## Краткий вывод

В 2026 году у IONOS Cloud заметны два разных класса проблем:

1. реальные деградации управляющего слоя — Managed Kubernetes, Cloud API и Data Center Designer;
2. ограничения емкости, когда уже работающий сервис доступен, но создать или повторно запустить ресурс нельзя.

Для production это разные риски: первый мешает управлению кластером, второй — масштабированию и восстановлению после остановки.

## Подтвержденные события

| Дата | Сервис | Что произошло | Воздействие |
| --- | --- | --- | --- |
| 11–12 августа | Provisioning, Cloud API, DCD | Увеличилось время обработки операций, соединения с DCD могли прерываться, Cloud API возвращал остаточные ошибки 500 | Создание и изменение ресурсов было затруднено; работающие VDC-ресурсы оставались доступны |
| 4–10 августа | Managed Kubernetes | Периодическая недоступность control plane, ошибки и тайм-ауты API | Уже запущенные workloads продолжали работать; управление кластером могло быть недоступно |
| 22–23 июня | Managed Kubernetes | Из-за высокого спроса в TXL и FRA временно остановили автоматическое обслуживание, требующее временного клонирования кластеров | Плановые обновления не выполнялись до расширения емкости |
| с 18 июня | GPU Server | Из-за дефицита емкости создание нового GPU-сервера или повторный запуск остановленного мог завершаться ошибкой | Провайдер прямо рекомендовал не выключать работающий GPU-сервер |
| с 12 июня | MongoDB DBaaS в `de/fra/2` | Playground и Business Edition нельзя было надежно создавать из-за ограничения емкости | Требовалась другая локация или Enterprise Edition |

Источник для актуальных записей: [IONOS Cloud Status](https://status.ionos.cloud/).

## Managed Kubernetes 4–10 августа

Провайдер связывал нестабильность control plane с нагрузкой и ограничениями памяти. В процессе восстановления:

- увеличивались лимиты памяти;
- расширялась базовая инфраструктура;
- клиентские control plane распределялись по дополнительным кластерам;
- выполнялась миграция на улучшенную инфраструктуру.

IONOS отдельно указал, что краткие перерывы control plane не должны были останавливать уже работающие workloads. Это важно: приложение могло продолжать обслуживать трафик, но `kubectl`, API и операции управления кластером — не работать.

## Provisioning 11–12 августа

Проблема относилась к управляющему слою:

- Data Center Designer мог терять соединение;
- операции provisioning выполнялись дольше;
- Cloud API периодически возвращал HTTP 500;
- Terraform, SDK и `ionosctl`, зависящие от API, также могли сталкиваться с ошибками.

Провайдер сообщил, что доступность уже созданных виртуальных дата-центров не пострадала.

## Ограничения емкости — не обычный простой

GPU и MongoDB могли показываться как частично недоступные не потому, что все существующие ресурсы остановились, а потому что отсутствовала свободная емкость для новых экземпляров.

Это создает отдельный риск восстановления: остановленный ресурс нельзя гарантированно запустить снова.

## Практический вывод

- для Kubernetes контролировать отдельно data plane и control plane;
- не считать работающий GPU гарантией возможности повторного запуска;
- перед аварийным восстановлением проверять доступную емкость в другой локации;
- IaC-пайплайн должен повторять операции после временных ошибок API;
- для важных кластеров иметь экспорт конфигурации и сценарий развертывания у другого провайдера.
